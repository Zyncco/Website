defaults:
  title: "Zync"
  description: "Secure clipboard syncing between your devices."
  keywords: "zync, cloud, encryption, clipboard, sync, secure"

sass:
  sass_dir: _scss
  style: compressed

url: "https://zync.co"